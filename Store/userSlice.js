const {createSlice} = require("@reduxjs/toolkit")

const initialState = {};

const userSlice = createSlice({
    name: "user",
    initialState,
    reducers : {
        getUser(){
            
        }
    }
})